"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeNetworkCaptureRules = void 0;
var tslib_1 = require("tslib");
/**
 * If a capture rule sets both `urls` and `hosts`, `urls` take precedence and `hosts` are ignored.
 */
var normalizeNetworkCaptureRules = function (captureRules, logger) {
    return captureRules === null || captureRules === void 0 ? void 0 : captureRules.map(function (rule) {
        var _a, _b;
        // if URLs and hosts are both set, URLs take precedence over hosts
        if (((_a = rule.urls) === null || _a === void 0 ? void 0 : _a.length) && ((_b = rule.hosts) === null || _b === void 0 ? void 0 : _b.length)) {
            var hostsString = JSON.stringify(rule.hosts);
            var urlsString = JSON.stringify(rule.urls);
            logger === null || logger === void 0 ? void 0 : logger.warn("Found network capture rule with both urls='".concat(urlsString, "' and hosts='").concat(hostsString, "' set. ") +
                "Definition of urls takes precedence over hosts, so ignoring hosts.");
            return tslib_1.__assign(tslib_1.__assign({}, rule), { hosts: undefined });
        }
        return rule;
    });
};
exports.normalizeNetworkCaptureRules = normalizeNetworkCaptureRules;
//# sourceMappingURL=network-tracking-config.js.map