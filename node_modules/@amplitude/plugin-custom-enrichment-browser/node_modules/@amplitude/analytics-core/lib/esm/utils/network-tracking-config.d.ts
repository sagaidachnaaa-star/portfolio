import { ILogger } from '../logger';
import { NetworkCaptureRule } from '../types/network-tracking';
/**
 * If a capture rule sets both `urls` and `hosts`, `urls` take precedence and `hosts` are ignored.
 */
export declare const normalizeNetworkCaptureRules: (captureRules: NetworkCaptureRule[] | undefined, logger?: Pick<ILogger, 'warn'> | null) => NetworkCaptureRule[] | undefined;
//# sourceMappingURL=network-tracking-config.d.ts.map