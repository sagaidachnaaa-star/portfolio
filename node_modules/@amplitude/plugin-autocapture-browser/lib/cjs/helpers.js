"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MouseButton = exports.resolveHistoryNavigationUrl = exports.getNormalizedPageUrl = exports.normalizePageUrl = exports.isElementBasedEvent = exports.filterOutNonTrackableEvents = exports.generateUniqueId = exports.asyncLoadScript = exports.resolveEventTarget = exports.querySelectorAllDeep = exports.getClosestElement = exports.querySelectUniqueElements = exports.getCurrentPageViewId = exports.removeEmptyProperties = exports.isEmpty = exports.extractPrefixedAttributes = exports.parseAttributesToMask = exports.isNonSensitiveElement = exports.isTextNode = exports.createShouldTrackEvent = exports.isUrlAllowed = exports.isElementPointerCursor = void 0;
var tslib_1 = require("tslib");
/* eslint-disable no-restricted-globals */
var analytics_core_1 = require("@amplitude/analytics-core");
var element_selector_1 = require("@amplitude/element-selector");
var constants = tslib_1.__importStar(require("./constants"));
var shadow_mode_1 = require("./shadow-mode");
var SENSITIVE_TAGS = ['input', 'select', 'textarea'];
var isElementPointerCursor = function (element, actionType) {
    var _a;
    /* istanbul ignore next */
    var computedStyle = (_a = window === null || window === void 0 ? void 0 : window.getComputedStyle) === null || _a === void 0 ? void 0 : _a.call(window, element);
    /* istanbul ignore next */
    return (computedStyle === null || computedStyle === void 0 ? void 0 : computedStyle.getPropertyValue('cursor')) === 'pointer' && actionType === 'click';
};
exports.isElementPointerCursor = isElementPointerCursor;
var isUrlAllowed = function (autocaptureOptions) {
    var pageUrlAllowlist = autocaptureOptions.pageUrlAllowlist, pageUrlExcludelist = autocaptureOptions.pageUrlExcludelist;
    // check if the URL is in the excludelist
    if (pageUrlExcludelist &&
        pageUrlExcludelist.length > 0 &&
        (0, analytics_core_1.isUrlMatchAllowlist)(window.location.href, pageUrlExcludelist)) {
        return false;
    }
    // check if the URL is in the allow list
    if (!(0, analytics_core_1.isUrlMatchAllowlist)(window.location.href, pageUrlAllowlist)) {
        return false;
    }
    return true;
};
exports.isUrlAllowed = isUrlAllowed;
var createShouldTrackEvent = function (autocaptureOptions, allowlist) {
    return function (actionType, element) {
        var _a, _b;
        var shouldTrackEventResolver = autocaptureOptions.shouldTrackEventResolver;
        /* istanbul ignore next */
        var tag = (_b = (_a = element === null || element === void 0 ? void 0 : element.tagName) === null || _a === void 0 ? void 0 : _a.toLowerCase) === null || _b === void 0 ? void 0 : _b.call(_a);
        // window, document, and Text nodes have no tag
        if (!tag) {
            return false;
        }
        if (shouldTrackEventResolver) {
            return shouldTrackEventResolver(actionType, element);
        }
        if (!(0, exports.isUrlAllowed)(autocaptureOptions)) {
            return false;
        }
        /* istanbul ignore next */
        var elementType = String(element === null || element === void 0 ? void 0 : element.getAttribute('type')) || '';
        if (typeof elementType === 'string') {
            switch (elementType.toLowerCase()) {
                case 'hidden':
                    return false;
                case 'password':
                    return false;
            }
        }
        /* istanbul ignore if */
        if (allowlist) {
            var hasMatchAnyAllowedSelector = allowlist.some(function (selector) { var _a; return !!((_a = element === null || element === void 0 ? void 0 : element.matches) === null || _a === void 0 ? void 0 : _a.call(element, selector)); });
            if (!hasMatchAnyAllowedSelector) {
                return false;
            }
        }
        switch (tag) {
            case 'input':
            case 'select':
            case 'textarea':
                return actionType === 'change' || actionType === 'click';
            default:
                return actionType === 'click';
        }
    };
};
exports.createShouldTrackEvent = createShouldTrackEvent;
var isTextNode = function (node) {
    return !!node && node.nodeType === 3;
};
exports.isTextNode = isTextNode;
var isNonSensitiveElement = function (element) {
    var _a, _b, _c;
    /* istanbul ignore next */
    var tag = (_b = (_a = element === null || element === void 0 ? void 0 : element.tagName) === null || _a === void 0 ? void 0 : _a.toLowerCase) === null || _b === void 0 ? void 0 : _b.call(_a);
    var isContentEditable = element instanceof HTMLElement ? ((_c = element.getAttribute('contenteditable')) === null || _c === void 0 ? void 0 : _c.toLowerCase()) === 'true' : false;
    return !SENSITIVE_TAGS.includes(tag) && !isContentEditable;
};
exports.isNonSensitiveElement = isNonSensitiveElement;
/**
 * Collects redacted attribute names from element and ancestor elements with data-amp-mask-attributes
 * The 'id' and 'class' attributes cannot be redacted as they're critical for element identification
 * @param element - The target element to check for redaction attributes
 * @returns Set of attribute names that should be redacted
 */
/**
 * Parses a comma-separated string of attribute names and filters out protected attributes
 * @param attributeString - Comma-separated string of attribute names
 * @returns Array of valid attribute names, excluding 'id' and 'class'
 */
var parseAttributesToMask = function (attributeString) {
    return attributeString
        ? attributeString
            .split(',')
            .map(function (attr) { return attr.trim(); })
            .filter(function (attr) { return attr.length > 0 && attr !== 'id' && attr !== 'class'; }) // Prevent 'id' and 'class' from being redacted as they're critical for element identification
        : [];
};
exports.parseAttributesToMask = parseAttributesToMask;
var extractPrefixedAttributes = function (attrs, prefix) {
    return Object.entries(attrs).reduce(function (attributes, _a) {
        var _b = tslib_1.__read(_a, 2), attributeName = _b[0], attributeValue = _b[1];
        if (attributeName.startsWith(prefix)) {
            var attributeKey = attributeName.replace(prefix, '');
            if (attributeKey) {
                attributes[attributeKey] = attributeValue || '';
            }
        }
        return attributes;
    }, {});
};
exports.extractPrefixedAttributes = extractPrefixedAttributes;
var isEmpty = function (value) {
    return (value === undefined ||
        value === null ||
        (typeof value === 'object' && Object.keys(value).length === 0) ||
        (typeof value === 'string' && value.trim().length === 0));
};
exports.isEmpty = isEmpty;
var removeEmptyProperties = function (properties) {
    return Object.keys(properties).reduce(function (filteredProperties, key) {
        var value = properties[key];
        if (!(0, exports.isEmpty)(value)) {
            filteredProperties[key] = value;
        }
        return filteredProperties;
    }, {});
};
exports.removeEmptyProperties = removeEmptyProperties;
var getCurrentPageViewId = function () {
    var _a;
    try {
        var globalScope = (0, analytics_core_1.getGlobalScope)();
        /* istanbul ignore next */
        var raw = (_a = globalScope === null || globalScope === void 0 ? void 0 : globalScope.sessionStorage) === null || _a === void 0 ? void 0 : _a.getItem(constants.PAGE_VIEW_SESSION_STORAGE_KEY);
        if (!raw) {
            return undefined;
        }
        var parsed = JSON.parse(raw);
        if (typeof parsed.pageViewId === 'string') {
            return parsed.pageViewId;
        }
    }
    catch (_b) {
        // ignore storage access or JSON errors
    }
    return undefined;
};
exports.getCurrentPageViewId = getCurrentPageViewId;
var querySelectUniqueElements = function (root, selectors) {
    if (root && 'querySelectorAll' in root && typeof root.querySelectorAll === 'function') {
        var elementSet = selectors.reduce(function (elements, selector) {
            if (selector) {
                var selectedElements = Array.from(root.querySelectorAll(selector));
                selectedElements.forEach(function (element) {
                    elements.add(element);
                });
            }
            return elements;
        }, new Set());
        return Array.from(elementSet);
    }
    return [];
};
exports.querySelectUniqueElements = querySelectUniqueElements;
// Similar as element.closest, but works with multiple selectors.
//
// Top-level dispatch: the light-DOM path is the pre-shadow implementation;
// shadow piercing is a separate walk.
var getClosestElement = function (element, selectors, shadow) {
    if (shadow === void 0) { shadow = shadow_mode_1.SHADOW_OFF; }
    if (shadow.enabled) {
        return getClosestElementInShadow(element, selectors, shadow.maxDepth);
    }
    return getClosestElementLight(element, selectors);
};
exports.getClosestElement = getClosestElement;
/** Pre-shadow behavior: `parentElement` only, stops at the tree boundary. */
var getClosestElementLight = function (element, selectors) {
    if (!element) {
        return null;
    }
    /* istanbul ignore next */
    if (selectors.some(function (selector) { var _a; return (_a = element === null || element === void 0 ? void 0 : element.matches) === null || _a === void 0 ? void 0 : _a.call(element, selector); })) {
        return element;
    }
    /* istanbul ignore next */
    return getClosestElementLight(element === null || element === void 0 ? void 0 : element.parentElement, selectors);
};
/** Shadow path: composed ancestor walk with a crossing budget. */
var getClosestElementInShadow = function (element, selectors, maxDepth) {
    var e_1, _a;
    if (!element) {
        return null;
    }
    var _loop_1 = function (current) {
        /* istanbul ignore next */
        if (selectors.some(function (selector) { var _a; return (_a = current === null || current === void 0 ? void 0 : current.matches) === null || _a === void 0 ? void 0 : _a.call(current, selector); })) {
            return { value: current };
        }
    };
    try {
        for (var _b = tslib_1.__values((0, element_selector_1.walkComposedAncestors)(element, maxDepth)), _c = _b.next(); !_c.done; _c = _b.next()) {
            var current = _c.value;
            var state_1 = _loop_1(current);
            if (typeof state_1 === "object")
                return state_1.value;
        }
    }
    catch (e_1_1) { e_1 = { error: e_1_1 }; }
    finally {
        try {
            if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
        }
        finally { if (e_1) throw e_1.error; }
    }
    return null;
};
/**
 * Like `querySelectUniqueElements` for a single joined selector, but also
 * queries open shadow roots up to `shadow.maxDepth` crossings. With
 * `SHADOW_OFF` this is a plain `querySelectorAll` on `root`.
 */
var querySelectorAllDeep = function (root, selectorString, shadow) {
    if (shadow === void 0) { shadow = shadow_mode_1.SHADOW_OFF; }
    return shadow.enabled
        ? querySelectorAllDeepInShadow(root, selectorString, shadow.maxDepth)
        : querySelectorAllDeepLight(root, selectorString);
};
exports.querySelectorAllDeep = querySelectorAllDeep;
var querySelectorAllDeepLight = function (root, selectorString) {
    var out = [];
    if (!selectorString) {
        return out;
    }
    root.querySelectorAll(selectorString).forEach(function (el) { return out.push(el); });
    return out;
};
var querySelectorAllDeepInShadow = function (root, selectorString, maxDepth) {
    var out = querySelectorAllDeepLight(root, selectorString);
    // `document.documentElement` is typed nullable (e.g. before parsing completes).
    // Shadow DFS needs an Element anchor; light-DOM matches were already collected above.
    var start = root instanceof Document ? root.documentElement : root;
    if (!start) {
        return out;
    }
    (0, element_selector_1.collectOpenShadowRoots)(start, maxDepth).forEach(function (_a) {
        var shadowRoot = _a.root;
        shadowRoot.querySelectorAll(selectorString).forEach(function (el) { return out.push(el); });
    });
    return out;
};
/**
 * The element an event originated from. Shadow DOM retargets `event.target` to
 * the shadow host when piercing is on; otherwise `event.target` is returned as-is.
 *
 * When piercing is enabled, the composed path is capped at `shadow.maxDepth`
 * shadow-boundary crossings — matching selector generation and observer indexing.
 * Clicks deeper than the budget resolve to the outermost in-budget shadow host.
 */
var resolveEventTarget = function (event, shadow) {
    var _a, _b;
    if (shadow === void 0) { shadow = shadow_mode_1.SHADOW_OFF; }
    if (!shadow.enabled) {
        return event.target;
    }
    var path = (_a = event.composedPath) === null || _a === void 0 ? void 0 : _a.call(event);
    var fallback = (_b = event.target) !== null && _b !== void 0 ? _b : null;
    if (!path || path.length === 0) {
        return fallback;
    }
    var innermost = path[0];
    if (!(innermost instanceof Element)) {
        return innermost !== null && innermost !== void 0 ? innermost : fallback;
    }
    var _c = (0, element_selector_1.segmentWalk)(innermost, shadow.maxDepth), segments = _c.segments, truncated = _c.truncated;
    if (truncated && segments.length > 0) {
        return segments[segments.length - 1].target;
    }
    return innermost;
};
exports.resolveEventTarget = resolveEventTarget;
var asyncLoadScript = function (url) {
    return new Promise(function (resolve, reject) {
        var _a;
        try {
            var scriptElement = document.createElement('script');
            scriptElement.type = 'text/javascript';
            scriptElement.async = true;
            scriptElement.src = url;
            scriptElement.addEventListener('load', function () {
                resolve({ status: true });
            }, { once: true });
            scriptElement.addEventListener('error', function () {
                reject({
                    status: false,
                    message: "Failed to load the script ".concat(url),
                });
            });
            /* istanbul ignore next */
            (_a = document.head) === null || _a === void 0 ? void 0 : _a.appendChild(scriptElement);
        }
        catch (error) {
            /* istanbul ignore next */
            reject(error);
        }
    });
};
exports.asyncLoadScript = asyncLoadScript;
function generateUniqueId() {
    return "".concat(Date.now(), "-").concat(Math.random().toString(36).substr(2, 9));
}
exports.generateUniqueId = generateUniqueId;
var filterOutNonTrackableEvents = function (event) {
    // Filter out changeEvent events with no target
    // This could happen when change events are triggered programmatically
    if (event.event.target === null || !event.closestTrackedAncestor) {
        return false;
    }
    return true;
};
exports.filterOutNonTrackableEvents = filterOutNonTrackableEvents;
// Type predicate
function isElementBasedEvent(event) {
    return event.type === 'click' || event.type === 'change';
}
exports.isElementBasedEvent = isElementBasedEvent;
/** Strip search params but keep path and hash so query-only changes are not treated as a new page. */
function normalizePageUrl(href) {
    if (!href) {
        return '';
    }
    try {
        var parsed = new URL(href);
        parsed.search = '';
        return (0, analytics_core_1.getDecodeURI)(parsed.href);
    }
    catch (_a) {
        return (0, analytics_core_1.getDecodeURI)(href);
    }
}
exports.normalizePageUrl = normalizePageUrl;
/** Normalized page URL used for viewport content and SPA navigation comparisons. */
function getNormalizedPageUrl(globalScope) {
    var _a, _b;
    if (globalScope === void 0) { globalScope = (0, analytics_core_1.getGlobalScope)(); }
    /* istanbul ignore next */
    return normalizePageUrl((_b = (_a = globalScope === null || globalScope === void 0 ? void 0 : globalScope.location) === null || _a === void 0 ? void 0 : _a.href) !== null && _b !== void 0 ? _b : '');
}
exports.getNormalizedPageUrl = getNormalizedPageUrl;
/** Resolve the post-navigation URL from a history API call. */
function resolveHistoryNavigationUrl(url, globalScope) {
    var _a;
    if (globalScope === void 0) { globalScope = (0, analytics_core_1.getGlobalScope)(); }
    if (url == null || url === '') {
        return getNormalizedPageUrl(globalScope);
    }
    /* istanbul ignore next */
    var baseHref = ((_a = globalScope === null || globalScope === void 0 ? void 0 : globalScope.location) === null || _a === void 0 ? void 0 : _a.href) || 'http://localhost/';
    try {
        return normalizePageUrl(new URL(url, baseHref).href);
    }
    catch (_b) {
        return getNormalizedPageUrl(globalScope);
    }
}
exports.resolveHistoryNavigationUrl = resolveHistoryNavigationUrl;
var MouseButton;
(function (MouseButton) {
    MouseButton[MouseButton["LEFT_OR_TOUCH_CONTACT"] = 0] = "LEFT_OR_TOUCH_CONTACT";
    MouseButton[MouseButton["MIDDLE"] = 1] = "MIDDLE";
    MouseButton[MouseButton["RIGHT"] = 2] = "RIGHT";
})(MouseButton = exports.MouseButton || (exports.MouseButton = {}));
//# sourceMappingURL=helpers.js.map