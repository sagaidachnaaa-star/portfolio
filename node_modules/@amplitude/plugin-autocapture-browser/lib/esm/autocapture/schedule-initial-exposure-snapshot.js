import * as constants from '../constants';
export function createInitialExposureSnapshotScheduler(_a) {
    var mutationObservable = _a.mutationObservable, onRescan = _a.onRescan, onFlush = _a.onFlush, exposureDuration = _a.exposureDuration, _b = _a.quietMs, quietMs = _b === void 0 ? constants.EXPOSURE_SNAPSHOT_QUIET_MS : _b, _c = _a.maxWaitMs, maxWaitMs = _c === void 0 ? constants.EXPOSURE_SNAPSHOT_MAX_WAIT_MS : _c;
    var snapshotTaken = false;
    var quietTimer;
    var maxWaitTimer;
    var exposureWaitTimer;
    var mutationSubscription;
    var domContentLoadedListener;
    var clearTimers = function () {
        if (quietTimer) {
            clearTimeout(quietTimer);
            quietTimer = undefined;
        }
        if (maxWaitTimer) {
            clearTimeout(maxWaitTimer);
            maxWaitTimer = undefined;
        }
        if (exposureWaitTimer) {
            clearTimeout(exposureWaitTimer);
            exposureWaitTimer = undefined;
        }
    };
    var removeDomContentLoadedListener = function () {
        if (domContentLoadedListener) {
            document.removeEventListener('DOMContentLoaded', domContentLoadedListener);
            domContentLoadedListener = undefined;
        }
    };
    var canSnapshot = function () {
        return typeof document !== 'undefined' && document.readyState !== 'loading';
    };
    var takeSnapshot = function () {
        /* istanbul ignore next */
        if (snapshotTaken) {
            return;
        }
        if (!canSnapshot()) {
            if (!domContentLoadedListener) {
                domContentLoadedListener = function () {
                    domContentLoadedListener = undefined;
                    // Hydration often starts at DCL; wait for a quiet window instead of snapshotting immediately.
                    scheduleQuietTimer();
                };
                document.addEventListener('DOMContentLoaded', domContentLoadedListener);
            }
            return;
        }
        snapshotTaken = true;
        clearTimers();
        removeDomContentLoadedListener();
        onRescan();
        exposureWaitTimer = setTimeout(function () {
            exposureWaitTimer = undefined;
            onFlush();
        }, exposureDuration + constants.EXPOSURE_SNAPSHOT_FLUSH_BUFFER_MS);
    };
    var scheduleQuietTimer = function () {
        if (snapshotTaken) {
            return;
        }
        if (quietTimer) {
            clearTimeout(quietTimer);
        }
        quietTimer = setTimeout(takeSnapshot, quietMs);
    };
    var arm = function () {
        clearTimers();
        removeDomContentLoadedListener();
        snapshotTaken = false;
        scheduleQuietTimer();
        maxWaitTimer = setTimeout(takeSnapshot, maxWaitMs);
    };
    return {
        start: function () {
            if (mutationSubscription) {
                return;
            }
            mutationSubscription = mutationObservable.subscribe(function () {
                scheduleQuietTimer();
            });
            arm();
        },
        reset: function () {
            arm();
        },
        stop: function () {
            mutationSubscription === null || mutationSubscription === void 0 ? void 0 : mutationSubscription.unsubscribe();
            mutationSubscription = undefined;
            clearTimers();
            removeDomContentLoadedListener();
        },
    };
}
//# sourceMappingURL=schedule-initial-exposure-snapshot.js.map